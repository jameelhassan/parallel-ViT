apiVersion: v1
kind: Pod
metadata:
  name: sanoojan-run-pod1
spec:
  restartPolicy: Never
  containers:
    - name: cuda-container
      image: '192.168.62.116:5000/jh-vit'
      command: ["sh", "/k8s_data/scripts0.sh"]
      # env:
      # - name: PATH
      #   value: "/opt/conda/bin:/home/sanoojan.baliah/.conda/envs/dist_vit/bin"
      resources:
        limits:
          nvidia.com/gpu: 2
      volumeMounts:
        - mountPath: /k8s_data
          name: volume
  volumes:
    - name: volume
      hostPath:
        path: /home/sanoojan.baliah/project/parallel-ViT
